; lib/daedalus-min.aura — Phase 5 facade + vision pipeline (issue #6)
;
; (require "daedalus-min" all:)
; Form order: export BEFORE require (#2766).

(export daed:min-version
        daed:stats
        daed:stats-reset!
        daed:stats-ref
        daed:stats-bump!
        daed:stats-set!
        daed:stats-alist
        daed:alist-ref
        daed:check-eq
        daed:check-true
        daed:check-approx
        daed:check-rel
        daed:escape-note!
        daed:escape-count
        ; netlist
        daed:netlist-version
        daed:comp daed:R daed:C daed:L daed:V daed:I daed:D daed:D* daed:Q daed:Q*
        daed:M daed:M*
        daed:E daed:G daed:F daed:H
        daed:circuit
        daed:ckt-name daed:ckt-comps
        daed:comp-type daed:comp-name daed:comp-n1 daed:comp-n2 daed:comp-n3
        daed:comp-n4 daed:comp-nc daed:comp-nb daed:comp-ne
        daed:comp-nd daed:comp-ng daed:comp-ns
        daed:comp-nc+ daed:comp-nc- daed:comp-ctrl daed:comp-terminals
        daed:comp-value daed:comp-set-value!
        daed:comp-set-n1! daed:comp-set-n2! daed:comp-set-n3! daed:comp-set-n4!
        daed:query-comp daed:query-by-type daed:query-nodes daed:query-names
        daed:comp-param daed:comp-set-param!
        daed:has-nonlinear?
        daed:max-node daed:count-type daed:node-index
        daed:n-voltage-unknowns
        daed:node-voltage daed:set-node-voltage! daed:x-v
        ; stamp
        daed:stamp-version daed:mna-size daed:mna-size-tran
        daed:needs-branch? daed:branch-map
        daed:stamp-system daed:stamp-system-tran
        daed:mat-ref daed:mat-set! daed:mat-add!
        daed:vec-ref daed:vec-set! daed:vec-add!
        daed:stamp-G! daed:stamp-I!
        ; devices / NR stamp
        daed:devices-version
        daed:clip-exp daed:pnjlim daed:diode-vcrit
        daed:stamp-system-nr daed:stamp-system-nr-tran
        daed:stamp-diode-nr! daed:stamp-bjt-nr! daed:stamp-mos-nr!
        ; solve
        daed:solve-version daed:dense-solve! daed:dense-solve-pure! daed:newton-solve
        daed:native-version daed:native-ready? daed:solve-backend
        daed:solve-rebind! daed:solve-rebind-ok? daed:solve-rebind-reason
        daed:solve-kernel-name daed:with-escape daed:rebind-safe
        daed:kernel-snapshot daed:kernel-restore! daed:own-check daed:own-ok?
        daed:native-bound?
        daed:dense-solve-native! daed:native-set-fail! daed:native-calls
        daed:ffi-version daed:load-kernels daed:bind-solve-dense
        daed:bind-c-func daed:ffi-ok? daed:ffi-reason daed:ffi-error
        daed:ffi-lib daed:ffi-fn daed:ffi-solve-dense!
        daed:simulate-op daed:simulate-op-from daed:simulate-op-gmin
        daed:simulate-op-gmin-step daed:simulate-op-source
        daed:simulate-op-ptran daed:simulate-op-aid
        daed:op-aid daed:op-aid-steps daed:op-aid-tried
        daed:op-ok? daed:op-x daed:op-n daed:op-n-v daed:op-voltages
        daed:op-iters daed:op-residual daed:op-i daed:op-ibranch
        daed:op-hist daed:op-fail-node daed:op-lambda
        daed:nr-maxiter daed:nr-atol daed:nr-vtol daed:nr-dvmax
        ; tran
        daed:tran-version
        daed:make-tran-state daed:nsteps-for daed:as-int daed:simulate-tran
        daed:simulate-tran-adapt
        daed:tran-ok? daed:tran-dt daed:tran-tstop daed:tran-nsteps
        daed:tran-times daed:tran-series daed:tran-v daed:tran-v-at
        daed:tran-v-at-t
        daed:tran-i daed:tran-i-at daed:tran-i-at-t daed:tran-i-series
        daed:tran-iters daed:tran-nr-fail
        daed:tran-accepted daed:tran-rejected daed:tran-dthist daed:tran-lte
        daed:tran-reltol daed:tran-abstol
        ; probe
        daed:probe-version daed:v daed:i daed:v-ground
        daed:check-voltage
        ; measure (issue #20)
        daed:measure-version
        daed:meas-ok? daed:meas-value daed:meas-t daed:meas-t2 daed:meas-kind
        daed:measure-max daed:measure-min daed:measure-avg daed:measure-rms
        daed:measure-final daed:measure-when daed:measure-delay
        daed:measure-risetime daed:measure-summary
        daed:tran->csv daed:write-tran-csv!
        ; step / temperature (issue #22)
        daed:step-version
        daed:temp daed:tnom daed:set-temp! daed:temp-k daed:vt-now daed:is-scale
        daed:step-lin daed:step-log daed:step-list
        daed:sweep-op daed:sweep-tran daed:sweep-temp-op daed:sweep-op-2
        daed:sweep-ok? daed:sweep-n daed:sweep-values daed:sweep-v
        daed:sweep-vend daed:sweep-best daed:sweep-waves
        daed:sweep-v1 daed:sweep-v2
        ; monte carlo (issue #23)
        daed:mc-version
        daed:rng-seed! daed:rng-state daed:rng-u01
        daed:rng-uniform daed:rng-gauss
        daed:dist-uniform daed:dist-gauss daed:dist-rel daed:dist-sample
        daed:mc-spec daed:mc-op daed:mc-tran
        daed:mc-ok? daed:mc-n daed:mc-mean daed:mc-std daed:mc-min daed:mc-max
        daed:mc-values daed:mc-trials daed:mc-trial daed:mc-yield daed:mc-robust?
        ; mutate (Phase 3)
        daed:mutate-version
        daed:find-comp daed:clone-comp daed:clone-circuit
        daed:topology-ok? daed:value-ok? daed:param-ok?
        daed:mutate-value! daed:mutate-param! daed:mutate-nodes!
        daed:mutate! daed:mutate-nodes-logged!
        daed:restore-circuit! daed:circuit-snapshot daed:comp-snapshot-value
        daed:snapshot daed:snapshot-sim daed:restore!
        daed:snap-ok? daed:snap-v daed:snap-i
        daed:mutlog daed:mutlog-reset! daed:mutlog-last
        daed:mutate-ok? daed:mutate-reason daed:mutate-old daed:mutate-new
        ; topology mutate (issue #25 / M4)
        daed:topo-version daed:n-comps daed:two-terminal?
        daed:topo-add! daed:topo-remove!
        daed:topo-series-r! daed:topo-unseries!
        daed:topo-parallel-c! daed:topo-parallel-r!
        daed:topo-comp-c! daed:topo-tap!
        daed:topo-op daed:topo-apply! daed:topo-search!
        daed:topo-ok? daed:topo-reason daed:topo-name
        daed:search-ok? daed:search-reached? daed:search-n
        daed:search-best daed:search-rejected
        daed:topo-note!
        ; spice export (issue #26 / M4)
        daed:spice-version daed:spice-subset
        daed:spice-tnom daed:spice-temp
        daed:circuit->spice daed:circuit->spice-tran daed:write-spice!
        ; agent (Phase 4)
        daed:agent-version
        daed:observe-v daed:err-abs
        daed:propose-r-scale daed:propose-divider-bottom-r
        daed:loop-once daed:agent-step! daed:autotune daed:autotune!
        daed:step-ok? daed:step-decision daed:step-v daed:step-reason
        daed:tune-ok? daed:tune-steps daed:tune-v daed:tune-reached?
        ; evolve (issue #24 / M4)
        daed:evolve-version
        daed:spec-v daed:spec-when daed:spec-risetime daed:spec-final
        daed:eval-spec daed:spec-ok? daed:spec-pass? daed:spec-err daed:spec-value
        daed:evo-try! daed:evolve daed:evolve!
        daed:evo-ok? daed:evo-reached? daed:evo-steps daed:evo-value daed:evo-err
        daed:evo-log daed:evo-n-good daed:evo-good
        daed:evo-restore-good! daed:evo-replay!
        ; viz (issue #1)
        daed:viz-version
        daed:string-has? daed:num->str
        daed:circuit->svg daed:circuit->html daed:html-after-op
        daed:apply-viz-edits!
        daed:viz-edit-ok? daed:viz-edit-applied daed:viz-edit-reason
        ; IR / validate / repair / vision (issue #6)
        daed:ir-version daed:ir-schema-id
        daed:parse-value daed:node-id
        daed:ir-new daed:ir-comp daed:ir-comp3 daed:ir-add-comp!
        daed:ir-title daed:ir-gnd daed:ir-comps
        daed:ir->circuit daed:circuit->ir
        daed:ir-ex-divider daed:ir-ex-rc daed:ir-ex-series-r
        daed:ir-ex-norton daed:ir-ex-diode
        daed:validate-version
        daed:issue daed:issue-error? daed:issues-errors daed:issues-error-count
        daed:validate-ir daed:validate-ckt
        daed:repair-version
        daed:rewrite-node!
        daed:repair-ensure-gnd! daed:repair-unshort-v! daed:repair-fix-value!
        daed:repair-flip-v! daed:repair-flip-d!
        daed:repair-once! daed:repair-ckt!
        daed:repair-ok? daed:repair-steps daed:repair-trace daed:repair-issues
        daed:vision-version
        daed:fixture-register! daed:fixture-lookup daed:install-builtin-fixtures!
        daed:basename daed:from-ir daed:from-image
        daed:pipe-ok? daed:pipe-circuit daed:pipe-sim daed:pipe-html
        daed:pipe-trace daed:pipe-reason)

(require "netlist" all:)
(require "stamp" all:)
(require "devices" all:)
(require "ffi" all:)
(require "native" all:)
(require "solve" all:)
(require "tran" all:)
(require "probe" all:)
(require "measure" all:)
(require "step" all:)
(require "mc" all:)
(require "mutate-circuit" all:)
(require "agent" all:)
(require "evolve" all:)
(require "topo" all:)
(require "spice" all:)
(require "viz" all:)
(require "ir" all:)
(require "validate" all:)
(require "repair" all:)
(require "vision" all:)

; 7 = M0 controlled sources E/G/F/H (issue #12)
(define daed:min-version 7)

(daed:install-builtin-fixtures!)

; ── Alist helper ──
(define (daed:alist-ref key lst default)
  (let loop ((ys lst))
    (cond ((null? ys) default)
          ((not (pair? (car ys))) (loop (cdr ys)))
          ((equal? (car (car ys)) key) (cdr (car ys)))
          (else (loop (cdr ys))))))

; ── Process-wide stats hash ──
(define (daed:make-stats-hash)
  (let ((h (hash)))
    (hash-set! h "correctness_ok" 0)
    (hash-set! h "correctness_fail" 0)
    (hash-set! h "escapes" 0)
    (hash-set! h "last_escape_reason" 0)
    (hash-set! h "sim_runs" 0)
    (hash-set! h "mutations" 0)
    (hash-set! h "rollbacks" 0)
    (hash-set! h "agent_rounds" 0)
    (hash-set! h "agent_commits" 0)
    (hash-set! h "agent_skips" 0)
    (hash-set! h "agent_refuses" 0)
    h))

(define *daed-stats* (daed:make-stats-hash))

(define (daed:stats)
  *daed-stats*)

(define (daed:stats-reset!)
  (set! *daed-stats* (daed:make-stats-hash))
  *daed-stats*)

(define (daed:stats-ref key)
  (let ((v (hash-ref *daed-stats* key 0)))
    (if (number? v) v 0)))

(define (daed:stats-bump! key)
  (let ((n (+ 1 (daed:stats-ref key))))
    (hash-set! *daed-stats* key n)
    n))

(define (daed:stats-set! key val)
  (hash-set! *daed-stats* key val)
  val)

(define (daed:stats-alist)
  (list (cons "correctness_ok" (daed:stats-ref "correctness_ok"))
        (cons "correctness_fail" (daed:stats-ref "correctness_fail"))
        (cons "escapes" (daed:stats-ref "escapes"))
        (cons "last_escape_reason" (daed:stats-ref "last_escape_reason"))
        (cons "sim_runs" (daed:stats-ref "sim_runs"))
        (cons "mutations" (daed:stats-ref "mutations"))
        (cons "rollbacks" (daed:stats-ref "rollbacks"))
        (cons "agent_rounds" (daed:stats-ref "agent_rounds"))
        (cons "agent_commits" (daed:stats-ref "agent_commits"))
        (cons "agent_skips" (daed:stats-ref "agent_skips"))
        (cons "agent_refuses" (daed:stats-ref "agent_refuses"))))

; Deep-ish clone of denseness metrics (dual rollback).
(define (daed:clone-stats)
  (let ((h (hash)))
    (hash-set! h "correctness_ok" (daed:stats-ref "correctness_ok"))
    (hash-set! h "correctness_fail" (daed:stats-ref "correctness_fail"))
    (hash-set! h "escapes" (daed:stats-ref "escapes"))
    (hash-set! h "last_escape_reason" (daed:stats-ref "last_escape_reason"))
    (hash-set! h "sim_runs" (daed:stats-ref "sim_runs"))
    (hash-set! h "mutations" (daed:stats-ref "mutations"))
    (hash-set! h "rollbacks" (daed:stats-ref "rollbacks"))
    (hash-set! h "agent_rounds" (daed:stats-ref "agent_rounds"))
    (hash-set! h "agent_commits" (daed:stats-ref "agent_commits"))
    (hash-set! h "agent_skips" (daed:stats-ref "agent_skips"))
    (hash-set! h "agent_refuses" (daed:stats-ref "agent_refuses"))
    h))

(define (daed:restore-stats! sh)
  (if (not sh)
    #f
    (begin
      (daed:stats-set! "correctness_ok" (hash-ref sh "correctness_ok" 0))
      (daed:stats-set! "correctness_fail" (hash-ref sh "correctness_fail" 0))
      (daed:stats-set! "escapes" (hash-ref sh "escapes" 0))
      (daed:stats-set! "last_escape_reason" (hash-ref sh "last_escape_reason" 0))
      (daed:stats-set! "sim_runs" (hash-ref sh "sim_runs" 0))
      (daed:stats-set! "mutations" (hash-ref sh "mutations" 0))
      (daed:stats-set! "rollbacks" (hash-ref sh "rollbacks" 0))
      (daed:stats-set! "agent_rounds" (hash-ref sh "agent_rounds" 0))
      (daed:stats-set! "agent_commits" (hash-ref sh "agent_commits" 0))
      (daed:stats-set! "agent_skips" (hash-ref sh "agent_skips" 0))
      (daed:stats-set! "agent_refuses" (hash-ref sh "agent_refuses" 0))
      #t)))

; ── Mutation log (queryable trail: kind → ok/reason) ──
(define *daed-mutlog* '())

(define (daed:mutlog-reset!)
  (set! *daed-mutlog* '())
  *daed-mutlog*)

(define (daed:mutlog)
  *daed-mutlog*)

(define (daed:mutlog-last)
  (if (null? *daed-mutlog*)
    #f
    (car *daed-mutlog*)))

(define (daed:mutlog-add! kind name ok reason)
  (let ((h (hash)))
    (hash-set! h "kind" kind)
    (hash-set! h "name" name)
    (hash-set! h "ok" ok)
    (hash-set! h "reason" reason)
    (set! *daed-mutlog* (cons h *daed-mutlog*))
    h))

; Lightweight solver-state clone: node volts + branch currents.
(define (daed:clone-op-lite res)
  (if (not res)
    #f
    (let ((h (hash))
          (volts (hash))
          (ibr (hash))
          (src-v (daed:op-voltages res))
          (src-i (daed:op-ibranch res))
          (k 0))
      (hash-set! h "ok" (hash-ref res "ok" #f))
      (hash-set! volts "0" 0.0)
      (set! k 1)
      (while (lambda () (< k 32))
        (lambda ()
          (let ((key (number->string k)))
            (let ((vv (hash-ref src-v key #f)))
              (if (number? vv)
                (hash-set! volts key vv)
                0)))
          (set! k (+ k 1))))
      (hash-set! h "volts" volts)
      (hash-set! h "ibranch" src-i)
      h)))

; ── Dual snapshot: circuit + denseness metrics (+ optional .op state) ──
; Optional host ast:snapshot id stored when available (>= 0).

(define (daed:snapshot ckt tag)
  (daed:snapshot-sim ckt tag #f))

(define (daed:snapshot-sim ckt tag sim)
  (let ((h (hash))
        (tg (if (string? tag) tag "daed"))
        (ast-id -1))
    (hash-set! h "tag" tg)
    (hash-set! h "circuit" (daed:circuit-snapshot ckt))
    (hash-set! h "stats" (daed:clone-stats))
    (hash-set! h "sim" (daed:clone-op-lite sim))
    (set! ast-id
      (try
        (let ((id (ast:snapshot tg)))
          (if (number? id) id -1))
        (catch (e) -1)))
    (hash-set! h "ast_id" ast-id)
    h))

(define (daed:snap-ok? snap)
  (let ((sim (hash-ref snap "sim" #f)))
    (if sim (hash-ref sim "ok" #f) #f)))

(define (daed:snap-v snap node)
  (let ((sim (hash-ref snap "sim" #f)))
    (if (not sim)
      0.0
      (hash-ref (hash-ref sim "volts" (hash)) (number->string node) 0.0))))

(define (daed:snap-i snap name)
  (let ((sim (hash-ref snap "sim" #f)))
    (if (not sim)
      0.0
      (hash-ref (hash-ref sim "ibranch" (hash)) name 0.0))))

; Restore circuit + metrics; then count a successful rollback.
(define (daed:restore! ckt snap)
  (if (not snap)
    #f
    (begin
      (daed:restore-circuit! ckt (hash-ref snap "circuit" #f))
      (daed:restore-stats! (hash-ref snap "stats" #f))
      (daed:stats-bump! "rollbacks")
      (daed:mutlog-add! "restore" (hash-ref snap "tag" "") #t "restored")
      #t)))

; Metered mutate: bumps "mutations" on success.
(define (daed:mutate! ckt name new-value)
  (let ((r (daed:mutate-value! ckt name new-value)))
    (daed:mutlog-add! "value" name
                      (hash-ref r "ok" #f)
                      (hash-ref r "reason" ""))
    (if (hash-ref r "ok" #f)
      (daed:stats-bump! "mutations")
      0)
    r))

(define (daed:mutate-nodes-logged! ckt name n1 n2)
  (let ((r (daed:mutate-nodes! ckt name n1 n2)))
    (daed:mutlog-add! "nodes" name
                      (hash-ref r "ok" #f)
                      (hash-ref r "reason" ""))
    (if (hash-ref r "ok" #f)
      (daed:stats-bump! "mutations")
      0)
    r))

(define (daed:mutate-ok? r)
  (hash-ref r "ok" #f))

(define (daed:mutate-reason r)
  (hash-ref r "reason" ""))

(define (daed:mutate-old r)
  (hash-ref r "old" 0))

(define (daed:mutate-new r)
  (hash-ref r "new" 0))

; ── Metered agent step / auto-tune (Phase 4) ──
; loop-once does circuit-only rollback; counters live here.
; Result accessors redefined here so facade free-refs stay solid (host export quirks).

(define (daed:step-ok? r)
  (hash-ref r "ok" #f))

(define (daed:step-decision r)
  (hash-ref r "decision" ""))

(define (daed:step-v r)
  (hash-ref r "v1" (hash-ref r "v0" 0.0)))

(define (daed:step-reason r)
  (hash-ref r "reason" ""))

(define (daed:tune-ok? r)
  (hash-ref r "ok" #f))

(define (daed:tune-steps r)
  (hash-ref r "steps" 0))

(define (daed:tune-v r)
  (hash-ref r "v" 0.0))

(define (daed:tune-reached? r)
  (hash-ref r "reached" #f))

(define (daed:agent-step! ckt node target tol name new-value)
  (daed:stats-bump! "agent_rounds")
  (let ((r (daed:loop-once ckt node target tol name new-value)))
    (let ((d (hash-ref r "decision" "")))
      (if (equal? d "commit")
        (begin
          (daed:stats-bump! "mutations")
          (daed:stats-bump! "agent_commits"))
        0)
      (if (equal? d "skip")
        (daed:stats-bump! "agent_skips")
        0)
      (if (equal? d "rollback")
        (daed:stats-bump! "rollbacks")
        0)
      (if (equal? d "refuse")
        (daed:stats-bump! "agent_refuses")
        0))
    r))

; Metered multi-step tune. strategy: "scale" | "analytic-divider"
(define (daed:autotune! ckt node target tol name max-steps strategy rtop-name vin)
  (let ((res (hash))
        (steps 0)
        (reached #f)
        (last #f)
        (strat (if (string? strategy) strategy "scale"))
        (lim (if (and (number? max-steps) (> max-steps 0)) max-steps 1)))
    (hash-set! res "target" target)
    (hash-set! res "tol" tol)
    (let loop ()
      (if (or reached (>= steps lim))
        (begin
          (let ((vf (daed:observe-v ckt node)))
            (daed:stats-bump! "sim_runs")
            (hash-set! res "v" (if (number? vf) vf 0.0))
            (hash-set! res "steps" steps)
            (hash-set! res "reached"
              (and (number? vf) (< (daed:err-abs vf target) tol)))
            (hash-set! res "ok" (hash-ref res "reached" #f))
            (hash-set! res "last" last)
            res))
        (let ((vnow (daed:observe-v ckt node)))
          (daed:stats-bump! "sim_runs")
          (if (and (number? vnow) (< (daed:err-abs vnow target) tol))
            (begin
              (set! reached #t)
              (daed:stats-bump! "agent_skips")
              (set! last
                (let ((h (hash)))
                  (hash-set! h "ok" #t)
                  (hash-set! h "decision" "skip")
                  (hash-set! h "reason" "within-tol")
                  (hash-set! h "v0" vnow)
                  (hash-set! h "v1" vnow)
                  h))
              (loop))
            (let ((prop
                    (if (equal? strat "analytic-divider")
                      (let ((rt (daed:find-comp ckt rtop-name)))
                        (if (not rt)
                          #f
                          (daed:propose-divider-bottom-r
                            (daed:comp-value rt) vin target)))
                      (let ((c (daed:find-comp ckt name)))
                        (if (or (not c) (not (number? vnow)) (= vnow 0.0))
                          #f
                          (* (daed:comp-value c) (/ target vnow)))))))
              (if (not (number? prop))
                (begin
                  (set! last
                    (let ((h (hash)))
                      (hash-set! h "ok" #f)
                      (hash-set! h "decision" "refuse")
                      (hash-set! h "reason" "no-proposal")
                      h))
                  (daed:stats-bump! "agent_refuses")
                  (set! steps lim)
                  (loop))
                (begin
                  (set! last
                    (daed:agent-step! ckt node target tol name prop))
                  (set! steps (+ steps 1))
                  (if (and (hash-ref last "ok" #f)
                           (equal? (hash-ref last "decision" "") "skip"))
                    (set! reached #t)
                    0)
                  (if (and (hash-ref last "ok" #f)
                           (equal? (hash-ref last "decision" "") "commit")
                           (< (daed:err-abs (hash-ref last "v1" 0.0) target) tol))
                    (set! reached #t)
                    0)
                  (if (equal? (hash-ref last "decision" "") "refuse")
                    (set! steps lim)
                    0)
                  (loop))))))))))

(define (daed:topo-note! r)
  (daed:mutlog-add! (hash-ref r "kind" "topo")
                    (hash-ref r "name" "")
                    (hash-ref r "ok" #f)
                    (hash-ref r "reason" ""))
  (if (hash-ref r "ok" #f)
    (daed:stats-bump! "mutations")
    0)
  r)

; Metered spec-driven search (issue #24).
(define (daed:evolve! ckt spec name max-steps strategy)
  (let ((r (daed:evolve ckt spec name max-steps strategy)))
    (let ((lg (daed:evo-log r))
          (i 0)
          (n 0))
      (set! n (vector-length lg))
      (while (lambda () (< i n))
        (lambda ()
          (let ((e (vector-ref lg i)))
            (let ((d (if e (hash-ref e "decision" "") "")))
              (daed:stats-bump! "agent_rounds")
              (daed:stats-bump! "sim_runs")
              (if (equal? d "commit")
                (begin
                  (daed:stats-bump! "mutations")
                  (daed:stats-bump! "agent_commits"))
                0)
              (if (equal? d "skip")
                (daed:stats-bump! "agent_skips")
                0)
              (if (equal? d "rollback")
                (daed:stats-bump! "rollbacks")
                0)
              (if (equal? d "refuse")
                (daed:stats-bump! "agent_refuses")
                0)))
          (set! i (+ i 1)))))
    r))

; ── Checks ──
(define (daed:check-eq actual expected)
  (if (and (number? actual) (number? expected) (= actual expected))
    (begin (daed:stats-bump! "correctness_ok") #t)
    (begin (daed:stats-bump! "correctness_fail") #f)))

(define (daed:check-true flag)
  (if flag
    (begin (daed:stats-bump! "correctness_ok") #t)
    (begin (daed:stats-bump! "correctness_fail") #f)))

(define (daed:check-approx actual expected tol)
  (if (and (number? actual)
           (number? expected)
           (number? tol)
           (< (abs (- actual expected)) tol))
    (begin (daed:stats-bump! "correctness_ok") #t)
    (begin (daed:stats-bump! "correctness_fail") #f)))

; Stats-aware relative check
(define (daed:check-rel actual expected tol floor-abs)
  (if (daed:check-rel-raw actual expected tol floor-abs)
    (begin (daed:stats-bump! "correctness_ok") #t)
    (begin (daed:stats-bump! "correctness_fail") #f)))

(define (daed:check-rel-raw actual expected tol floor-abs)
  (and (number? actual)
       (number? expected)
       (number? tol)
       (number? floor-abs)
       (let ((den (abs expected)))
         (let ((d (if (> den floor-abs) den floor-abs)))
           (< (/ (abs (- actual expected)) d) tol)))))

; ── Escape metering ──
(define (daed:escape-note! reason-tag)
  (daed:stats-bump! "escapes")
  (daed:stats-set! "last_escape_reason" reason-tag)
  reason-tag)

(define (daed:escape-count)
  (daed:stats-ref "escapes"))

(daed:native-on-escape! daed:escape-note!)
